<h1>Projects</h1>

<ul>
  <% projects.forEach(project => { %>
    <li><a href="<%= projectPath(project.id) %>"><%= project.title %></a></li>
  <% }) %>
</ul>

<a href="<%= newProjectPath %>">new project</a>