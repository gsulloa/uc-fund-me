const fileStorage = require('./file-storage');
/* eslint-disable */

describe('file storage service', () => [
  it('create file storage works', () => {
    expect(fileStorage).toBeTruthy();
  })
])

/* eslint-enable */
